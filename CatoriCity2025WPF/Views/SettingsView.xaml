<Window x:Class="CatoriCity2025WPF.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CatoriCity2025WPF.Views"
        xmlns:uc="clr-namespace:CatoriCity2025WPF.Views.Controls"
        mc:Ignorable="d" WindowStartupLocation="CenterOwner"
        Title="Settings View" Height="600" Width="1024" Closing="Window_Closing" Background="{DynamicResource MainGradient}">
    <Grid Background="{DynamicResource MainGradient}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="GroupIdsPanel" Grid.Row="0" Panel.ZIndex="1200" Orientation="Horizontal">
            <Label x:Name="GroupsLabel" Content="Groups"/>
            <StackPanel x:Name="GroupsIdsList" Orientation="Horizontal"/>
        </StackPanel>
        <TabControl x:Name="SettingsTabControl" Grid.Row="1" Background="{DynamicResource MainGradient}">
            <TabItem Header="General">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="109.975" />
                        <ColumnDefinition Width="17.088"/>
                        <ColumnDefinition Width="42.937"/>
                        <ColumnDefinition Width="50" />
                        <ColumnDefinition Width="170" />
                        <ColumnDefinition Width="50" />
                    </Grid.ColumnDefinitions>
                    <Button x:Name="AddNewSetting" Width="50" Content="New" Click="AddNewSetting_Click" Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"/>
                    <DataGrid Name="Settingsgrid" Grid.Row="1" Grid.ColumnSpan="6" Width="800" AutoGenerateColumns="False" SelectionChanged="Settingsgrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding SettingID}" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                            <DataGridTextColumn Header="String Setting" Binding="{Binding StringSetting}" />
                            <DataGridTextColumn Header="Int Setting" Binding="{Binding IntSetting}" />
                        </DataGrid.Columns>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="DarkSlateGray"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Landscape Groups" HorizontalAlignment="Left" Width="109">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="109.975" />
                        <ColumnDefinition Width="17.088"/>
                        <ColumnDefinition Width="42.937"/>
                        <ColumnDefinition Width="50" />
                        <ColumnDefinition Width="170" />
                        <ColumnDefinition Width="50" />
                    </Grid.ColumnDefinitions>
                    <Button x:Name="AddNewLandscapeItem" Width="50" Content="New" Click="AddNewLandscapeItem_Click"  Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"/>
                    <DataGrid Name="LandscapeGroups" Grid.Row="1" Grid.ColumnSpan="6" AutoGenerateColumns="False" SelectionChanged="LandscapeGroups_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding LandScapeObjectID}" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                            <DataGridTextColumn Header="Image Name" Binding="{Binding ImageName}" />
                            <DataGridTextColumn Header="Height" Binding="{Binding Height}" />
                            <DataGridTextColumn Header="Width" Binding="{Binding Width}" />
                            <DataGridTextColumn Header="xActual" Binding="{Binding xActual}" />
                            <DataGridTextColumn Header="yActual" Binding="{Binding yActual}" />
                            <DataGridTextColumn Header="xPercent" Binding="{Binding xPercent}" />
                            <DataGridTextColumn Header="yPercent" Binding="{Binding yPercent}" />
                        </DataGrid.Columns>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="DarkSlateGray"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Colors">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30" />
                                <RowDefinition Height="30" />
                                <RowDefinition Height="30" />
                                <RowDefinition Height="30" />
                                <RowDefinition Height="30" />
                                <RowDefinition Height="30" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="UI Element"/>
                            <Label Content="Color" Grid.Column="1"/>
                            <Label Content="Screen Background Color" Grid.Row="1"/>
                            <uc:ColorPickerUC x:Name="ScreenBackgroundColorPicker" Grid.Row="1" Grid.Column="1" />
                        </Grid>
                    </TabItem>
            <TabItem Header="Tests">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="30" />
                        <RowDefinition Height="30" />
                        <RowDefinition Height="30" />
                        <RowDefinition Height="30" />
                        <RowDefinition Height="30" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200" />
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="PoliceCarsButton" Content="Police Cars" Click="PoliceCarsButton_Click"  Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"/>
                    <Button x:Name="BadPersonButton" Content="Bad person" Grid.Row="1" Click="BadPersonButton_Click"  Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"/>
                </Grid>
            </TabItem>
            <TabItem Header="Misc">
                <Grid>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="BanksButton" Content="Banks" Click="BanksButton_Click"  Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"/>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
        
        <Button x:Name="SaveSettingsButton" Grid.Row="2" Width="50" Click="SaveSettingsButton_Click">
            <Image x:Name="SaveImage" Source="/Images/SaveButtonCool.png" Width="1720" Height="50"/>
        </Button>
    </Grid>
</Window>

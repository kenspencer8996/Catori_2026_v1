<Window x:Class="CatoriApp.MachineLayoutDesigner.Views.MachineCatalog.MachineCatalogListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Machine Catalog"
        Height="760"
        Width="1160"
        WindowState="Maximized"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Loaded="Window_Loaded"
        Background="{DynamicResource MainGradient}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="60" />
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Content="Machine Catalog" Style="{DynamicResource RedBoldLabelStyle}"  />

        <Button x:Name="NewMachineButton"
                Grid.Column="1"
                Content="New"
                MinWidth="50"
                Width="100"
                Height="25"
                Click="NewMachineButton_Click"
                Background="{DynamicResource ButtonBackground}"
                Foreground="{DynamicResource ButtonForeground}" />
        <Button x:Name="ExitButton"
                Grid.Column="4"
                Style="{StaticResource MapHotspotButton}"
                Width="50"
                Height="50"
                Click="ExitButton_Click"
                ToolTip="Click to exit"
                Panel.ZIndex="1002" />

        <DataGrid x:Name="MainDataGrid"
                  Grid.Row="1"
                  Grid.ColumnSpan="5"
                  Background="{DynamicResource MainGradient}"
                  ItemsSource="{Binding Definitions}"
                  SelectedItem="{Binding SelectedDefinition, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding MachineType, UpdateSourceTrigger=PropertyChanged}" Width="160" />
                <DataGridTextColumn Header="Name" Binding="{Binding MachineName, UpdateSourceTrigger=PropertyChanged}" Width="220" />
                <DataGridTextColumn Header="Default Width" Binding="{Binding DefaultWidth, UpdateSourceTrigger=PropertyChanged}" Width="110" />
                <DataGridTextColumn Header="Default Height" Binding="{Binding DefaultHeight, UpdateSourceTrigger=PropertyChanged}" Width="110" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="*" />
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick" />
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <TextBlock Grid.Row="2"
                   Grid.ColumnSpan="5"
                   Text="{Binding StatusMessage}"
                   TextWrapping="Wrap"
                   Margin="8,6,8,8" />
    </Grid>
</Window>
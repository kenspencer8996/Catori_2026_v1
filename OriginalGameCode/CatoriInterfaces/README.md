# CatoriInterfaces

Dependency-free contracts shared by CatoriApp, CatoriStudio, services, controls, and tests.

Keep implementations and application-specific dependencies out of this project so all Catori projects can reference it without introducing circular dependencies.

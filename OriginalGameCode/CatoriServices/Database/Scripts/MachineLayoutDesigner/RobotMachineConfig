CREATE TABLE RobotMachineConfig (
    MachineId INTEGER PRIMARY KEY,
    SegmentCount INTEGER NOT NULL DEFAULT 4,
    MaxReach REAL NULL,
    DefaultPoseName TEXT NULL,

    FOREIGN KEY (MachineId) REFERENCES Machine(MachineId)
);
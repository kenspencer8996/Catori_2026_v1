<Window x:Class="CatoriApp.Game.Views.Locations.Warehouse.WarehouseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CatoriApp.Game.Views.Locations.Warehouse"
        mc:Ignorable="d"
        Title="WarehouseView"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize">
    <Grid ClipToBounds="True">
        <Image x:Name="WarehouseImage"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch"
               Stretch="UniformToFill" />
        <ContentControl x:Name="LocationUCPanel"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch" />
        <Button x:Name="ExitButton"
                Style="{StaticResource MapHotspotButton}"
                Width="50"
                Height="50"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,8,8,0"
                Click="ExitButton_Click"
                ToolTip="Click to exit"
                Panel.ZIndex="10002" />
    </Grid>
</Window>

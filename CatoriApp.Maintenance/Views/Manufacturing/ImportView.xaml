<Window x:Class="CatoriApp.Game.Views.Manufacturing.ImportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manufacturing Import"
        Height="320"
        Width="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MainGradient}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Import" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,12" />
        <UniformGrid Grid.Row="1" Columns="2" Rows="2">
            <Button Content="Products" Margin="6" Click="ProductImport_Click" Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}" />
            <Button Content="BOM" Margin="6" Click="BOMImport_Click" Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}" />
            <Button Content="Components" Margin="6" Click="ComponentImport_Click" Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}" />
            <Button Content="Inventory" Margin="6" Click="InventoryImport_Click" Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}" />
        </UniformGrid>
        <DockPanel Grid.Row="2" Margin="0,12,0,0">
            <Button DockPanel.Dock="Right" Content="Close" Width="90" Click="CloseButton_Click" Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}" />
            <TextBlock x:Name="StatusTextBlock" Foreground="White" TextWrapping="Wrap" VerticalAlignment="Center" />
        </DockPanel>
    </Grid>
</Window>